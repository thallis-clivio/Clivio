kind = "web"
previewPath = "/"
title = "Creatives Dashboard"
version = "1.0.0"
id = "artifacts/creatives-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25630

[services.development]
run = "pnpm --filter @workspace/creatives-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/creatives-dashboard", "run", "build" ]
publicDir = "artifacts/creatives-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25630"
BASE_PATH = "/"
